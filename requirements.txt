# Neuro SAN
# Pinned, not floored: leaf-common 1.3.x drops the leaf_common.session package that
# leaf-server-common 0.1.25 imports, so a fresh install leaves the server unable to start
# (ModuleNotFoundError: No module named 'leaf_common.session'). 1.2.52 is the version this
# neuro-san was released against.
leaf-common==1.2.52
neuro-san==0.6.92
nsflow==0.6.17

# For config parsing
pyhocon>=0.3.60

# For the airline_policy demo, to extract text from documents
pypdf>=6.1.3

# To use a .env file for environment variables
python-dotenv>=1.2.2,<2.0.0

# For asynchronous file operations
aiofiles>=24.1.0

# For asynchronous HTTP requests
aiohttp>=3.13.0,<4.0

# For HTML parsing
beautifulsoup4>=4.12.0

# For MCP servers and clients
langchain-mcp-adapters>=0.1.7,<1.0

# Neuro-SAN now requires explicit installation of langchain LLM providers' libs
langchain-openai>=1.1.9,<2.0
langchain-anthropic>=1.4.3,<2.0
langchain-google-genai>=4.2.2,<5.0

# anthropic arrives transitively via langchain-anthropic and was previously unconstrained, so a
# fresh resolve takes a version that no longer ships anthropic.types.beta.file_metadata. That
# module is imported by neuro_san_studio/coded_tools/anthropic_code_execution.py, so pylint fails
# it with import-error and CI's lint-check breaks on code nobody touched. Verified by inspecting
# the wheels: 0.123.0 still has the module, 0.124.0 removed it. Pinning is the stopgap; the real
# fix is updating that import to the module's new home.
anthropic<0.124

langchain-community>=0.4.2

# For rich logging
rich>=14.2.0

# For the HTML agent-network graph generator
pyvis~=0.3.2

# For web search tools without needing API keys, using DuckDuckGo's search results
ddgs>=9.4.1

# For the neuro-san-studio CLI
typer>=0.12.0,<1.0

# For chat-based cli tools not used in deployment
# Also used by neuro-san
timedinput>=0.1.1

# For checkbox-based selection in CLI
questionary>=2.0.0

# ReservationsStorage
# ... for S3
botocore>=1.34.51
aiobotocore>=3.7.0
